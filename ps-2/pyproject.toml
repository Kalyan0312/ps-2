[project]
name = "lidar-adaptive-mapping"
version = "1.0.0"
requires-python = ">=3.12,<3.15"
dependencies = [
    "fastapi",
    "uvicorn",
    "numpy",
    "pydantic"
]

[tool.vercel]
entrypoint = "backend.api.main:app"
